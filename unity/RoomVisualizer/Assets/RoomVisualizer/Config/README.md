# Config

Place ScriptableObject assets here:

- `AssetLibraryConfig.asset` — maps AI Pipeline category IDs to local glTF/GLB asset paths
  and provides per-entry metadata (DisplayName, Thumbnail, Category) for the Object Palette.

Create via: Assets > Create > RoomVisualizer > Asset Library Config
