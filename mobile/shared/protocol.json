{
  "protocol_version": "1.0",
  "description": "Room Vision AI Bluetooth transfer protocol schema",
  "message_types": ["pairing_request", "pairing_response", "image_transfer", "transfer_ack", "error"],
  "envelope": {
    "protocol_version": "string",
    "message_type": "string",
    "timestamp": "ISO 8601 string",
    "payload": "object (type-specific)"
  }
}
